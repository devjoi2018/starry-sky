const CANVAS_WIDTH = 800;
const CANVAS_HEIGHT = 600;

const STAR_COLOR = '#FFFFFF';
const STAR_MIN_SIZE = 0.3;  // Aumentado de 0.2
const STAR_MAX_SIZE = 1.2;  // Aumentado de 0.8

const ANIMATION_DURATION = 1000; // Duración en milisegundos
const ANIMATION_INTERVAL = 200; // Intervalo entre animaciones en milisegundos

const NUM_STARS = 150;      // Reducido para mejor rendimiento

export {
    CANVAS_WIDTH,
    CANVAS_HEIGHT,
    STAR_COLOR,
    STAR_MIN_SIZE,
    STAR_MAX_SIZE,
    ANIMATION_DURATION,
    ANIMATION_INTERVAL,
    NUM_STARS
};